# 更新日志

本文件记录 ToneCore 的版本变更。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：
主版本号变化代表不兼容的配置或数据变更，次版本号代表新增功能，修订号代表缺陷修复。

---

## [0.5.0] - 2026-09-22

参照 Navidrome / SongLoft 主流结构重做页面：导航收敛为「主页 / 曲库 / 音源 / 设置」四项，
主页承载歌单与播放，曲库承载检索、详情与刮削，搜索入口不再各自为政。

### 新增

- **歌单（命名的可持久化队列）**：新增 `playlists` / `playlist_items` 表与全套接口
  （列表、新建、删除、详情、加曲、删曲、`/api/playlists/:id/play` 整张播放）。
  主页展示歌单网格，曲库中可把任意本地歌曲「加入歌单」。
- **主页 = 歌单 + 播放**：默认落地页。顶部全网搜索框（内联在线结果，不跳页）；
  右侧常驻「正在播放」面板（封面 / 进度 / 传输控制 / 队列）；下方歌单网格 + 最近添加条
  （新用户也能直接播放，避免空态）。
- **曲库 = 检索 + 详情 + 刮削**：移除曲库里的「正在播放」侧栏（已归主页）。
  顶部全网搜索框与右上角独立的「本地搜索」小框分离，互不打扰；
  点开歌曲弹出详情（专辑 / 封面 / 时长 / 路径），可「重新刮削这首」
  （新增 `/api/scraper/song` 单首刮削接口，写入标签 / 封面 / 歌词并刷新封面缓存）。
- **导航四项**：侧栏与移动端底部 Tab 统一为 主页 / 曲库 / 音源 / 设置；
  「运行状态」折叠进侧栏底部状态条，「在线搜索」不再单独成页。

### 变更

- 默认路由由「音乐库」改为「主页」。
- 联网搜索结果渲染抽成 `OnlineSearch` 组件，主页与曲库共用，消除重复代码。

---

## [0.4.0] - 2026-09-22

把「正在播放」整合进音乐库，听歌不必在两个页面之间来回跳；搜索入口收敛为一个。

### 新增

- **播放页整合进音乐库**：原来「正在播放」是独立一页，听歌时要在「播放页 ↔ 曲库」
  之间来回切换，不符合 Navidrome / SongLoft 一类主流播放器的通行结构。
  现在曲库即主页面：右侧常驻「正在播放」面板（封面、进度、传输控制、播放队列），
  左侧点即播；全屏歌词与常驻迷你条继续承担沉浸式播放。
  桌面端为「列表 + 侧栏」双栏布局，窄屏自动降为单栏，由迷你条与全屏页接管。
- **统一搜索条**：曲库页原本只有一个「本地搜索」输入框，与顶栏全局搜索、
  在线搜索页三处并存，入口冗余。现收敛为一个搜索条，用「本地 / 在线」分段切换：
  本地过滤曲库，在线走 `/api/search` 联网搜歌并可直接试听、入队。
  在线搜索当前为**预留接入**，取链依赖音源健康度。

### 变更

- 移除已无引用的独立「正在播放」视图（其能力已并入曲库侧栏与全屏播放页）。

---

## [0.3.0] - 2026-09-22

修复音源取链「全军覆没」级别的根因缺陷，并顺带收紧取链链路的健壮性。

### 修复

- **音源取链全部 30s 超时（致命）**：宿主派发任务后只等脚本回叫
  `lx.send('request', {requestKey, data})`，但洛雪主流脚本的 `request` 处理器是
  **同步返回 Promise**（`return handleGetMusicUrl(...)`），根本不回叫。于是每一路取链
  都必然等满 30s 超时，表现为「所有音源都取不到直链」。
  改为**同时接受两条回填路径**——处理器返回的 Promise（主流）与脚本显式
  `send`（少数/旧实现）——谁先到用谁，另一个自动作废。
  修复后 Free listen、HYWmusic、全豆要、念心 等音源恢复秒级取链。
- **取链链路健壮性**：`invoke` 返回值现在做了统一的幂等结算（只 resolve/reject 一次），
  避免「Promise 与 send 双路回填」导致某路永远悬挂；超时计时器在结算时一并清除。

---

## [0.2.0] - 2026-09-21

从「无头中枢」升级为**自带完整播放器**：控制台改为侧边栏导航，新增本地播放与
歌词滚动，并修掉一批评分、封面、歌词链路上的真实缺陷。

### 新增

- **内置播放器**：完整的播放队列（上一首 / 下一首 / 跳转 / 局部移除 / 清空）、
  暂停继续、进度拖动、音量调节、三种循环模式（列表 / 单曲 / 随机）。
  默认首页即「正在播放」，桌面端下方常驻迷你播放条，移动端可展开全屏播放页。
- **歌词**：LRCLIB 与网易云双源兜底，解析标准 LRC 时间轴并支持滚动高亮。
  纯音乐无歌词或拿不到歌词时明确降级，不显示假数据。
- **在线试听与歌曲搜索页**：搜到的结果可直接试听、入队、落库，不必先去音箱点歌。
- **曲库封面**：扫描时抽取音频内嵌封面，落成独立小图缓存（`data/covers`），
  列表与播放器直接展示，避免每页重复解析几十 MB 的音频文件。
- **控制台改版**：右上角配置入口改为**左侧侧边栏**（正在播放 / 曲库 / 音源 / 设置），
  桌面端常驻、移动端抽屉式，命名对齐主流音乐播放器习惯。
- **音源在线检测**：单个音源可一键实测「能否取到直链」，结果含耗时与失败原因。

### 修复

- **点错歌**：打分公式里「标题完全匹配」与「部分匹配」只差 0.27 分，
  版本噪音一叠加就会被翻唱反超。改为拉开分值区间并加入平台质量权重，
  同时在分数过低时**拒绝降级返回**，宁可提示失败也不放错歌。
- **点播长时间卡住**：高分候选取链失败后会逐个串行重试，每次都等满超时，
  实测「孤勇者」要 125 秒才失败。改为限制单候选取链时长并降低并发重试成本。
- **音源检测全部超时**：检测走的是洛雪 `search` 动作，而洛雪脚本只实现
  `musicUrl`（取直链），所以每个音源都必然超时。改为按真实能力检测取链。
- **封面与歌词大面积缺失**：原依赖的 `api.lrc.cx` 搜索与封面接口**已整体下线**
  （返回 404），导致没有内嵌封面的歌永远补不上。改用可达的音源并增加多候选兜底。
- **内部封面抽取未生效**：抽取函数漏了 `await`，异步结果被当成对象读取，
  于是**每首歌都被判定为「无封面」**并写入占位标记，之后不再重试。
  已修正并让历史占位数据能自动重试补齐。
- **音源脚本可能拖垮宿主**：部分混淆脚本在加载期抛出的**异步**异常逃逸出
  同步 `try/catch`，会连带影响同进程内其他脚本。已在沙箱内做异步异常隔离。

### 变更

- **下线失效平台**：咪咕接口改版返回 HTML、酷狗存在证书校验失败，
  两者搜索结果恒为空，已从搜索平台与默认平台顺序中移除。
  接口响应会明确标注已下线平台，避免被误读成「搜索坏了」。
- **音源清理**：移除加载即失败的失效音源脚本（保留在 `_disabled` 便于恢复）。
- **图标换新**：改为亮色调（底色亮度约翻倍），并提供矢量源文件便于后续调整。

### 文档

- 补全 CHANGELOG、RELEASES 与版本发布说明。
- 部署文档补充播放器相关目录说明与移动端适配要点。

首个版本。ToneCore 定位为**无头音乐中枢**：不做播放器界面，只负责把「想听的歌」
送到小爱音箱，并在这个过程中把音源沉淀成本地曲库。

### 新增

- **语音点歌链路**：轮询音箱对话记录，解析「播放周杰伦的晴天」这类自然语言，
  提取歌名与歌手后走本地曲库 → 全网音源 → 推送播放 → 异步落库。
- **播放控制**：语音与接口双通道支持上一首 / 下一首 / 暂停 / 继续 / 停止，
  以及音量绝对设置与相对增减（「大声点」「音量调到 50」）。
- **音源引擎**：跨平台统一打分竞争。把各平台（酷我 / 酷狗 / QQ / 网易 / 咪咕）
  的候选放在一起按标题相似度、歌手匹配度、版本噪音词综合评分，
  避免「按固定平台顺序取第一个」导致点到翻唱。支持失败降级与低分拒绝。
- **无损落库**：异步下载队列，串行 + 固定间隔防风控，落盘前做音频魔数校验
  （识别 FLAC / MP3 / AAC / Ogg / WAV / APE / M4A / WMA），
  拒绝把 HTML 错误页当音频存下来。
- **自动刮削**：落库后主动补齐元数据 —— 写入 ID3v2 / Vorbis Comment 标签、
  嵌入专辑封面、写入歌词（标签 + 同名 `.lrc`）。另支持对已有曲库批量补全。
- **本地曲库**：SQLite 索引，支持关键词检索、分页、试听、统计，
  删除走回收站（`/data/_trash`）而非硬删。
- **音源脚本管理**：加载洛雪音乐格式的音源脚本，带健康度评分与连续失败熔断，
  连续失败次数超阈值自动跳过；支持启停（移入 `_disabled/`）、删除（移入 `_trash/`）、
  单源连通性测试，加载失败原因在界面直接可见。
- **控制台**：Vue 3 + Tailwind 深色界面，含总览 / 音源 / 曲库 / 设置四个页面，
  PC 与移动端双端适配。
- **容器交付**：多阶段 Docker 构建，产出 amd64 / arm64 双架构镜像；
  附 Unraid 容器模板。

### 修复

- 容器模板中暴露的 `QUALITY`、`AUTO_FETCH`、`DOWNLOAD_CONCURRENCY`、
  `DOWNLOAD_INTERVAL_MS`、`LOG_LEVEL` 环境变量此前未被代码读取，
  在 Unraid 上修改音质等参数不生效。
- 音箱对话轮询使用单个共享时间戳，多台设备同时在线时，
  较新设备的时间戳会抬高游标，导致其他设备时间戳更早的指令被静默丢弃
  （表现为「只有一台音箱能点歌」）。
- 曲库扫描的「新增数量」统计依赖独立的 `SELECT changes()`，
  该值可能被中间语句冲掉，导致新增计数不可靠。
- `/stream` 的目录穿越防护只校验字符串前缀，
  形如 `/music-evil` 的相邻路径可绕过；改为按路径分隔符校验。
- `/stream` 对目录路径会返回 200 而非 404。

### 变更

- 版本号收敛到单一出处（`apps/server/src/version.ts`），
  此前在健康检查接口与前端徽章中各自硬编码。
- 配置优先级明确为「环境变量 > config.yaml > 内置默认值」，
  被环境变量锁定的字段会在设置页显式提示。
- 配置读取增加取值归一与别名（如「无损」→ `flac`）以及越界截断，
  防止手写配置把运行期搞坏。
- **运行镜像基线从 Node 20 升到 Node 22**。`better-sqlite3` 从 12.x 起
  已停止为 Node 20（ABI 115）提供预编译产物，导致镜像构建时回退到源码编译；
  在 arm64 交叉编译（QEMU）下这一步耗时极长且易失败。Node 22（ABI 127）
  有官方 musl 预编译产物，构建恢复为直接下载。
- 构建产物增加原生模块自检：`better-sqlite3` 无法加载时在构建期即失败，
  不再等到容器运行、首次读写曲库时才暴露。
- CI 增加单架构构建 + 启动冒烟测试（健康检查 + 曲库接口），
  通过后再进行双架构正式推送，避免「镜像推成功了但容器起不来」。
- 仓库图标重新设计：音符 + 声波，圆角方底，蓝金配色；
  同时提供矢量源文件 `assets/icon.svg`。

### 文档

- 补全 CHANGELOG、RELEASES 与版本发布说明。
- 重写部署文档，补充 Unraid 模板用法与目录说明。
- 开发文档补充发版流程、测试方式与配置优先级说明。

---

[0.1.0]: https://github.com/deltrivx/ToneCore/releases/tag/v0.1.0
